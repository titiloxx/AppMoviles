/*
function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}

async function sleepGD() {
    await sleep(500);
}
async function sleepGrande() {
    await sleep(5000);
}

    sleepGrande();
    while (true) {
        var string = $("#main_slider").attr("data-slideactive");
        if (string != $("#main_slider").attr("data-slideactive")) {
            var string2 = $("#main_slider").attr("data-slideactive");
            break;
        }
        sleepGD();
    }

    while (true) {
        if ($("#main_slider").attr("data-slideactive") == string2) {
            $("#1").show();
            $("#2").hide();
        }
        else {
            $("#1").hide();
            $("#2").show();
        }
        sleepGD();
    }
    */
 


